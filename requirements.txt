# CodeLooter Dependencies
#
# Install dengan: pip install -r requirements.txt
#
# Semua dependency di bawah ini GRATIS dan ringan — compatible dengan
# Render free tier (512MB RAM, shared CPU, no GPU).
#
# TIDAK ada LLM, TIDAK ada PyTorch, TIDAK ada TensorFlow.
# Ekstraksi murni menggunakan pattern matching (regex) yang sudah
# dioptimalkan dengan pre-processing pipeline.

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# PDF extraction (murni Python, ~20MB)
pymupdf>=1.23.0

# Database
psycopg2-binary>=2.9.0
supabase>=2.0.0

# Auth
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# Rate limiting
slowapi>=0.1.9

# NOTE: Tidak ada sentence-transformers/PyTorch/TensorFlow.
# Ekstraksi kode 100% menggunakan pattern matching (regex) yang sudah
# dioptimalkan. Lihat backend/app/pattern_extract.py untuk detail.
